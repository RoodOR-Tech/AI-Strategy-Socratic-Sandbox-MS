# AI Strategy Socratic Sandbox

A static workshop app for guiding an agency AI adoption strategy session.

## Deploy on Vercel

1. In Vercel, choose **Add New Project**.
2. Import this GitHub repository.
3. Use the default static-site settings. No build command is required.

The app runs entirely in the browser and stores workshop notes locally.